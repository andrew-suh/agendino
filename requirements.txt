fastapi[standard]
python-dotenv
jinja2
google-genai
json-repair
httpx
chromadb
icalendar
recurring-ical-events
mutagen
faster-whisper
scikit-learn
# sentence-transformers is only needed for EMBEDDING_PROVIDER=local (in-process embeddings,
# non-Docker dev). The Docker image uses EMBEDDING_PROVIDER=ollama, so it's left out to avoid
# pulling torch (~2-3GB). For local dev: pip install sentence-transformers
celery[redis]
redis
anthropic
